Absolutely. Now that **Person 1 = Frontend, Person 2 = Backend, Person 3 = Database**, you are effectively **Person 4**.

And I would **not** give you a small leftover task like "AI chatbot." Your role should be the **AI + Integration + Intelligence + Final System Integration Lead** because that is where you can create the strongest differentiation for the hackathon.

# 🚀 IRISiv — PERSON 4 COMPLETE ROLE & IMPLEMENTATION DESCRIPTION

## Your Role

You are the **AI & System Integration Lead for IRISiv**.

Your responsibility is to build the intelligence layer of IRISiv and make sure the four components work together as **one complete product**.

Your work covers:

* AI Proposal Evaluation
* AI Delivery Verification
* AI Impact/Report Generation
* AI Assistant
* AI-related integrations
* Notification/event integration where required
* Frontend ↔ Backend ↔ Database ↔ AI integration
* Demo orchestration
* End-to-end system testing
* Final hackathon presentation/demo readiness

You are **NOT responsible for rebuilding the frontend, backend, or database**.

---

# 1. YOUR POSITION IN THE ARCHITECTURE

The final system should look like:

```text
                    IRISiv
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
    FRONTEND       BACKEND       DATABASE
   Person 1       Person 2       Person 3
        │             │             │
        └─────────────┼─────────────┘
                      ↓
                  YOU / P4
                      │
             AI + INTEGRATION LAYER
                      │
          ┌───────────┼───────────┐
          ↓           ↓           ↓
     AI Proposal   AI Delivery   AI Impact
     Evaluation    Verification   Analysis
          │           │           │
          └───────────┼───────────┘
                      ↓
                AI Assistant
```

Your job is to make AI **useful inside the actual CSR workflow**, rather than adding random AI features.

---

# 2. THE MOST IMPORTANT RULE

## AI MUST NOT BECOME A SEPARATE FEATURE

Do **not** build:

```text
Dashboard
+
Random AI Chatbot
```

Instead build:

```text
CSR Workflow
      ↓
Relevant Data
      ↓
AI Analysis
      ↓
Human Decision
      ↓
Workflow Continues
```

The project's AI is supposed to assist verification and decision-making rather than independently control critical financial decisions. 

---

# 3. YOUR CORE AI MODULES

You should implement **four major AI capabilities**.

## Module 1 — AI Proposal Evaluation

When a business submits a proposal:

```text
Business
   ↓
Proposal
   ↓
Backend
   ↓
AI Evaluation
   ↓
Scores + Recommendation
   ↓
Corporate Dashboard
```

AI should evaluate factors such as:

* Cost
* Timeline
* Capacity
* Experience
* Feasibility

Output:

```json
{
  "cost_score": 92,
  "timeline_score": 90,
  "capacity_score": 95,
  "experience_score": 91,
  "feasibility_score": 94,
  "overall_score": 92.4,
  "recommendation": "Strong Candidate",
  "reasoning": "..."
}
```

### Important

AI recommends.

Corporate decides.

Never:

```text
AI → Automatically select business
```

Correct:

```text
AI
 ↓
Recommendation
 ↓
Corporate
 ↓
Final Selection
```

---

# 4. AI PROPOSAL COMPARISON

This is where your AI becomes impressive during the demo.

Suppose Corporate receives:

```text
Business A
₹48,000
14 days
Score: 91

Business B
₹45,000
20 days
Score: 84

Business C
₹51,000
10 days
Score: 93
```

AI can produce:

```text
Recommended Candidate

Business C

Why?

• Fastest delivery
• Strong capacity
• High feasibility
• Slightly higher cost justified by timeline
```

The Corporate dashboard then visually shows:

```text
┌─────────────────────────────────────┐
│ AI EVALUATION                       │
│                                     │
│ Recommended: Business C             │
│                                     │
│ Overall Score          93           │
│ Cost                  88            │
│ Timeline              97            │
│ Capacity              94            │
│ Experience            92            │
│ Feasibility            95           │
│                                     │
│ AI Recommendation                   │
│ Strong Candidate                    │
└─────────────────────────────────────┘
```

This connects your AI directly to Person 1's frontend.

---

# 5. MODULE 2 — AI DELIVERY VERIFICATION

This is arguably the **most important AI feature** in IRISiv.

The workflow:

```text
Business
   ↓
Delivery
   ↓
Evidence
   ↓
NGO Verification
   ↓
AI Verification
   ↓
Corporate Review
```

AI receives:

```text
Project Requirements
+
Requested Quantity
+
Delivery Quantity
+
NGO Verification
+
Evidence Metadata
```

Then evaluates consistency.

---

# 6. AI VERIFICATION OUTPUT

Example:

```json
{
  "status": "LIKELY_FULFILLED",
  "confidence": 0.96,
  "requested_quantity": 500,
  "received_quantity": 500,
  "completion_percentage": 100,
  "issues": [],
  "recommendation": "Review for final payment"
}
```

Frontend can show:

```text
AI VERIFICATION

✓ Likely Fulfilled

Confidence
96%

Requested
500

Received
500

Completion
100%

Issues
None

Recommendation
Review for final payment
```

---

# 7. AI MISMATCH DETECTION

You MUST also implement the failure scenario.

Otherwise your AI will look like a simple "everything is successful" demo.

Example:

```text
Requested Quantity: 1000

Received Quantity: 950
```

AI:

```text
⚠ ISSUE DETECTED

Completion: 95%

Shortfall: 50 units

Confidence: 94%

Recommendation:
Manual Review Required
```

Workflow:

```text
AI detects mismatch
        ↓
MANUAL REVIEW
        ↓
Corporate reviews
        ↓
Decision
        ↓
Continue / resolve
```

**Never automatically complete the project because AI says everything is okay.**

---

# 8. MODULE 3 — AI IMPACT REPORT

Once a project is completed:

```text
Completed Project
       ↓
Project Data
       ↓
AI Impact Analysis
       ↓
Impact Report
```

AI can generate a structured summary using actual database values.

Example:

```text
CSR IMPACT REPORT

Project:
School Kit Distribution

Beneficiaries:
500

Requested:
500 kits

Delivered:
500 kits

Completion:
100%

Verification:
AI + NGO Verified

Impact:
The project successfully delivered the planned
school kits to 500 beneficiaries within the
approved execution window.
```

### Critical rule

AI must **not invent metrics**.

If database says:

```text
500 beneficiaries
```

AI can summarize 500.

It cannot decide:

```text
"Impact increased education outcomes by 37%"
```

unless the system actually has evidence supporting that.

---

# 9. MODULE 4 — AI ASSISTANT

The AI assistant should not just be:

> "Hello, how can I help?"

It should understand the **IRISiv system context**.

For example, Corporate asks:

> "Which projects need my attention?"

AI should be able to use authorized system data and respond:

```text
You have 3 projects requiring attention:

1. CSR-1024
   AI verification detected a 5% quantity mismatch.

2. CSR-1027
   NGO verification is pending.

3. CSR-1031
   Final payment review is pending.
```

---

# 10. ROLE-AWARE AI ASSISTANT

The assistant must respect roles.

### NGO asks:

> "What projects need verification?"

AI answers from NGO-authorized projects.

### Corporate asks:

> "Which projects have verification issues?"

AI answers from Corporate-authorized projects.

### Business asks:

> "What's my current project status?"

AI answers only for that business's projects.

Never allow:

```text
Business → Ask AI → Get another company's private data
```

---

# 11. AI ASSISTANT ARCHITECTURE

Use:

```text
User
 ↓
Frontend AI Assistant
 ↓
Backend AI endpoint
 ↓
Authentication
 ↓
Authorization
 ↓
Relevant project data
 ↓
AI service
 ↓
Structured response
 ↓
Frontend
```

Do **not** allow the frontend to send unrestricted database contents directly to an AI model.

---

# 12. AI SERVICE ARCHITECTURE

Create a clean abstraction.

For example:

```text
AIService
│
├── evaluateProposal()
│
├── verifyDelivery()
│
├── generateImpactReport()
│
└── answerAssistantQuery()
```

The backend should call your service.

Do not scatter model/API calls throughout the application.

---

# 13. AI MODEL PROVIDER

The exact model can depend on the project's agreed stack.

Possible architecture:

```text
IRISiv
  ↓
AI Adapter
  ↓
Gemini / OpenAI / other approved provider
```

The important part is:

**Do not tightly couple the entire application to one AI provider.**

For example:

```text
ai/
   services/
      proposalEvaluation.ts
      deliveryVerification.ts
      impactReport.ts
      assistant.ts

   providers/
      gemini.ts

   types/
      proposal.ts
      verification.ts
```

Then another model provider can be introduced later without rewriting the whole application.

---

# 14. STRUCTURED AI OUTPUT

Do not depend on free-form AI text wherever the result affects application logic.

Bad:

```text
"The project seems mostly complete and probably okay..."
```

Better:

```json
{
  "status": "ISSUE_DETECTED",
  "confidence": 0.94,
  "completion_percentage": 95,
  "issues": [
    {
      "code": "QUANTITY_MISMATCH",
      "expected": 1000,
      "actual": 950,
      "shortfall": 50
    }
  ],
  "recommendation": "MANUAL_REVIEW_REQUIRED"
}
```

Then the frontend can reliably display it.

---

# 15. AI VALIDATION

Never trust AI output directly.

Flow:

```text
AI Model
   ↓
Raw Response
   ↓
Schema Validation
   ↓
Business Validation
   ↓
Database
```

For example:

AI returns:

```text
completion_percentage = 150
```

Your system must reject it.

Valid:

```text
0 ≤ completion_percentage ≤ 100
```

Likewise:

```text
0 ≤ confidence ≤ 1
```

---

# 16. AI FAILURE HANDLING

AI APIs can fail.

You must handle:

* Timeout
* Invalid response
* Rate limit
* API failure
* Malformed JSON
* Missing fields
* Model unavailable
* Network error

The system must not break the CSR workflow.

Example:

```text
AI unavailable
      ↓
Store verification as:
AI_VERIFICATION_PENDING / FAILED
      ↓
Notify user
      ↓
Allow manual review
```

Never:

```text
AI failed
 ↓
Crash entire project
```

---

# 17. AI SHOULD NOT CONTROL MONEY

This is extremely important.

AI must never directly execute:

```text
Release payment
```

Instead:

```text
AI Verification
       ↓
Recommendation
       ↓
Corporate Review
       ↓
Backend validation
       ↓
Payment state
```

This preserves the human-in-the-loop architecture.

---

# 18. SYSTEM INTEGRATION

This is your other major responsibility.

You need to connect:

```text
Person 1
Frontend
     ↓
Person 2
Backend
     ↓
Person 3
Database
     ↓
You
AI
```

You should test the actual integration rather than testing AI independently.

---

# 19. END-TO-END FLOW YOU MUST MAKE WORK

You should be able to demonstrate:

```text
NGO
 ↓
Create Requirement
 ↓
Submit
 ↓
Corporate
 ↓
Approve
 ↓
Publish Opportunity
 ↓
Business
 ↓
Submit Proposal
 ↓
AI
 ↓
Evaluate Proposal
 ↓
Corporate
 ↓
Compare Proposals
 ↓
Select Business
 ↓
Contract
 ↓
Advance Payment State
 ↓
Business
 ↓
Start Project
 ↓
Submit Delivery
 ↓
Upload Evidence
 ↓
NGO
 ↓
Verify
 ↓
AI
 ↓
Verify Delivery
 ↓
Corporate
 ↓
Review
 ↓
Final Payment State
 ↓
Completed
 ↓
AI
 ↓
Generate Impact Report
```

**This is your final integration test.**

---

# 20. EVENT-BASED INTEGRATION

Where useful, connect AI to backend events.

For example:

```text
PROPOSAL_SUBMITTED
        ↓
AI Evaluation
```

and:

```text
NGO_VERIFICATION_SUBMITTED
        ↓
AI Verification
```

and:

```text
PROJECT_COMPLETED
        ↓
Impact Report Generation
```

The exact implementation can be synchronous or asynchronous depending on complexity and hackathon time.

---

# 21. DO NOT OVERENGINEER

For the hackathon, don't build:

❌ Microservices
❌ Kubernetes
❌ Complex event brokers
❌ Blockchain infrastructure
❌ Real banking integrations
❌ Complex ML training pipelines

unless specifically required.

Your goal is:

> **A reliable AI-assisted CSR workflow that can be demonstrated end-to-end.**

---

# 22. BLOCKCHAIN — YOUR ROLE

If blockchain is part of the final presentation, keep it as a **trust/audit demonstration**, not as something that complicates the core workflow.

Possible architecture:

```text
Important Event
      ↓
Backend
      ↓
Hash / Verification Record
      ↓
Blockchain / Test Network
```

For example:

```text
Project Completed
      ↓
Create event hash
      ↓
Store verification reference
```

But:

**Do not make blockchain a dependency for the basic project lifecycle.**

The project should still work if the blockchain integration is unavailable.

---

# 23. NOTIFICATION INTEGRATION

You can also own notification orchestration.

Examples:

```text
New Proposal
      ↓
Corporate notification

Delivery Submitted
      ↓
NGO notification

Verification Complete
      ↓
Corporate notification

Business Selected
      ↓
Business notification

Project Completed
      ↓
All relevant participants notified
```

The database stores the notification.

Backend determines when it should be created.

Your integration layer can help trigger external notification services if required.

---

# 24. DEMO DATA / DEMO ORCHESTRATION

This is another area I strongly recommend you own.

Create a reliable demo scenario.

Instead of hoping everything is manually configured during the hackathon:

```text
Demo Reset
   ↓
Seed Data
   ↓
Known Users
   ↓
Known Projects
   ↓
Known Proposals
   ↓
Known AI Results
```

Prepare at least:

```text
CSR-1021 → SUBMITTED
CSR-1022 → PROPOSALS_OPEN
CSR-1023 → BUSINESS_SELECTED
CSR-1024 → IN_PROGRESS
CSR-1025 → AI_VERIFIED
CSR-1026 → COMPLETED
```

This gives you different screens to demonstrate.

---

# 25. YOUR HACKATHON DEMO STORY

You should not explain IRISiv by showing 20 random screens.

Tell one story.

Example:

### Scene 1 — NGO

```text
"We have a CSR requirement."
```

Create:

```text
School Kit Distribution
500 beneficiaries
₹50,000 budget
```

---

### Scene 2 — Corporate

Corporate sees:

```text
New CSR Requirement
```

Approves it.

Publishes opportunity.

---

### Scene 3 — Business

Business sees:

```text
Open Opportunity
```

Submits proposal.

---

### Scene 4 — AI

Your AI immediately evaluates:

```text
Business A — 88
Business B — 93
Business C — 81
```

Show the recommendation.

Then say:

> "AI recommends Business B, but the final decision remains with the corporate."

That's a **very important judge-facing point**.

---

### Scene 5 — Execution

Business gets selected.

Starts project.

Submits:

```text
500 kits
```

with evidence.

---

### Scene 6 — Verification

NGO says:

```text
Received: 500
```

AI checks:

```text
Requested: 500
Received: 500

Confidence: 96%

Likely Fulfilled
```

---

### Scene 7 — Mismatch Scenario

For extra impact:

Change the delivery:

```text
Requested: 1000
Received: 950
```

AI:

```text
⚠ Quantity mismatch

95% completion

Manual review required
```

Then explain:

> "Instead of blindly releasing funds, IRISiv creates a human review checkpoint."

This is much stronger than simply showing an AI chatbot.

---

# 26. YOUR DOCUMENTATION

You should create these files:

```text
AI-SPEC.md
AI-ARCHITECTURE.md
AI-PROMPTS.md
AI-OUTPUT-SCHEMAS.md
AI-ERROR-HANDLING.md
AI-ASSISTANT-SPEC.md
INTEGRATION-SPEC.md
DEMO-FLOW.md
```

### `AI-SPEC.md`

Defines every AI capability.

### `AI-ARCHITECTURE.md`

Defines how AI connects to backend.

### `AI-PROMPTS.md`

Contains controlled prompts.

### `AI-OUTPUT-SCHEMAS.md`

Defines structured JSON outputs.

### `AI-ERROR-HANDLING.md`

Defines what happens when AI fails.

### `AI-ASSISTANT-SPEC.md`

Defines role-aware assistant behavior.

### `INTEGRATION-SPEC.md`

Defines frontend/backend/database/AI integration.

### `DEMO-FLOW.md`

Defines your final judge demonstration.

---

# 27. SECURITY

You are also responsible for AI-related security.

Never send:

* passwords
* auth tokens
* secrets
* service-role keys
* unrelated private user information

to the AI model.

Only send the minimum authorized data required.

---

# 28. COST CONTROL

During development:

Do not repeatedly call paid AI APIs unnecessarily.

Use:

```text
Mock AI
 ↓
Development
 ↓
Real AI
 ↓
Final Demo
```

where appropriate.

Cache deterministic demo evaluations if necessary.

---

# 29. PERFORMANCE

Don't make the dashboard wait unnecessarily for multiple AI calls.

For example:

```text
Corporate Dashboard
```

should not require:

```text
20 AI calls
```

just to load.

Instead:

```text
Dashboard
 ↓
Database
 ↓
Existing AI evaluation results
```

AI should run when the relevant event occurs.

---

# 30. YOUR DEFINITION OF DONE

You are finished only when:

### AI

* [ ] Proposal evaluation works
* [ ] Proposal scoring works
* [ ] AI recommendation works
* [ ] Delivery verification works
* [ ] Quantity mismatch works
* [ ] Confidence works
* [ ] Impact report generation works
* [ ] AI assistant works
* [ ] Structured output validated
* [ ] AI failures handled

### Integration

* [ ] Frontend receives AI results
* [ ] Backend triggers AI correctly
* [ ] Database stores AI results
* [ ] AI assistant respects authorization
* [ ] Notifications work
* [ ] End-to-end lifecycle works

### Demo

* [ ] Demo data exists
* [ ] Successful case exists
* [ ] Failure/mismatch case exists
* [ ] AI recommendation can be shown
* [ ] Human-in-the-loop verification can be shown
* [ ] Impact report can be shown

---

# 31. YOUR DEVELOPMENT ORDER

Follow this order.

## PHASE 1 — Understand the system

Read:

```text
PRD.md
USER-FLOW.md
PERSON-1-HANDOFF.md
PERSON-2-HANDOFF.md
DATABASE-SCHEMA.md
```

Understand the entire workflow.

---

## PHASE 2 — AI contracts

Create:

```text
AI-SPEC.md
AI-OUTPUT-SCHEMAS.md
```

Define inputs/outputs first.

---

## PHASE 3 — Proposal AI

Implement:

```text
evaluateProposal()
```

Test with known proposals.

---

## PHASE 4 — Verification AI

Implement:

```text
verifyDelivery()
```

Test:

```text
1000 → 1000
```

and:

```text
1000 → 950
```

---

## PHASE 5 — Impact AI

Implement:

```text
generateImpactReport()
```

using actual project data.

---

## PHASE 6 — AI Assistant

Implement:

```text
answerAssistantQuery()
```

with role-aware data access.

---

## PHASE 7 — Integration

Connect:

```text
Backend
 ↓
AI
 ↓
Database
 ↓
Frontend
```

---

## PHASE 8 — Demo

Create:

```text
Successful Project
+
Problematic Project
+
Completed Project
```

and test the complete journey.

---

# 32. FINAL RESPONSIBILITY

Your job is **not**:

> "Make some AI features."

Your actual job is:

> **Make IRISiv intelligent.**

Person 1 makes it **look good**.

Person 2 makes it **work**.

Person 3 makes it **store and protect the truth**.

**You make it intelligent, integrated, demonstrable, and judge-ready.**

The strongest architecture is therefore:

```text
                 IRISiv
                   │
        ┌──────────┼──────────┐
        ↓          ↓          ↓
     FRONTEND    BACKEND    DATABASE
       P1          P2          P3
        │          │           │
        └──────────┼───────────┘
                   ↓
                 YOU
                   │
          AI + INTEGRATION
                   │
       ┌───────────┼───────────┐
       ↓           ↓           ↓
   Proposal     Delivery     Impact
   Evaluation   Verification Report
       │           │           │
       └───────────┼───────────┘
                   ↓
             AI ASSISTANT
                   ↓
            JUDGE DEMONSTRATION
```

### Your golden rule

**Never add AI just because "the project needs AI."**

Every AI feature must answer:

> **"At which exact point in the CSR workflow does this AI capability create measurable value?"**

If you can point to that exact step in the dashboard and demonstrate the before → AI analysis → human decision → outcome, **that's what will make your project feel like a real system rather than a collection of dashboards and AI features.**
